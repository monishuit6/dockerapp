apiVersion: v1
kind: Pod
metadata:
  name: docker-app-pod
  labels:
    name: docker-app-pod
    app: demo-docker-app
spec:
  containers:
    - name: docker-app
      image: monish1092/audiencepixel:latest
      ports:
        - containerPort: 80
